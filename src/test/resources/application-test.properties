# Local data source
#spring.datasource.url=${ipa.datasource.test.url}
#spring.datasource.type=com.zaxxer.hikari.HikariDataSource
#spring.datasource.hikari.username=${dw.datasource.test.username}
#spring.datasource.hikari.password=${dw.datasource.test.password}

# Logging
logging.level.org.springframework=DEBUG
