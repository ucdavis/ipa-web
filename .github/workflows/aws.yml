on:
  push:
    branches:
      - rtt-272-auto-deploy

name: Push image to ECR and force new ECS deploy

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-west-2

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: PWD
        run: 'pwd'
        shell: bash

      - name: Copy root pem
        run: 'echo $RDS_ROOT_PEM | base64 -d > rds-ca-2019-root.pem'
        shell: bash
        env:
          RDS_ROOT_PEM: ${{secrets.RDS_ROOT_PEM}}

      - name: Copy us-west-2 pem
        run: 'echo $RDS_US_WEST_2_PEM | base64 -d > rds-ca-2019-us-west-2.pem'
        shell: bash
        env:
          RDS_US_WEST_2_PEM: ${{secrets.RDS_US_WEST_2_PEM}}

      - name: Copy dw cer
        run: 'echo $DW_CER | base64 -d > dw.dss.ucdavis.edu.cer'
        shell: bash
        env:
          DW_CER: ${{secrets.DW_CER}}

      - name: LS
        run: 'ls'
        shell: bash

      - name: temp
        run: 'cat rds-ca-2019-root.pem'
        shell: bash

      - name: temp2
        run: 'cat rds-ca-2019-us-west-2.pem'
        shell: bash

      - name: Build and redeploy
        uses: jaroldwong/ecr-push-and-ecs-deploy@v1.1
        with:
          ecr-registry: ${{ steps.login-ecr.outputs.registry }}
          ecr-repository: 'ipa-web-staging'
          ecs-cluster: 'legacy'
          ecs-service: 'ipa-web-staging'
